<!DOCTYPE html>
<html lang="ur">
<head>
<meta charset="UTF-8">
<title>نیا سال مبارک ہو</title>
<style>
body {
    margin: 0;
    height: 100vh;
    background: radial-gradient(circle at bottom, #001f3f, #000);
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    overflow: hidden;
    font-family: Arial, sans-serif;
    color: white;
    text-align: center;
}

.balloons {
    display: flex;
    gap: 20px;
    margin-bottom: 30px;
}

.balloon {
    width: 80px;
    height: 100px;
    border-radius: 50%;
    position: relative;
    animation: float 2s infinite ease-in-out;
}

.balloon:nth-child(1){background:red;}
.balloon:nth-child(2){background:yellow;}
.balloon:nth-child(3){background:green;}

.balloon::after {
    content: "";
    width: 2px;
    height: 40px;
    background: white;
    position: absolute;
    bottom: -40px;
    left: 50%;
}

@keyframes float {
    0%,100% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
}

#message {
    font-size: 48px;
    display: none;
    animation: zoom 1s ease forwards;
}

@keyframes zoom {
    from {transform: scale(0);}
    to {transform: scale(1);}
}

canvas {
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
}
</style>
</head>
<body>

<div class="balloons" id="balloons">
    <div class="balloon"></div>
    <div class="balloon"></div>
    <div class="balloon"></div>
</div>

<div id="message">🎉 نیا سال مبارک ہو 🎉</div>

<canvas id="fireworks"></canvas>

<audio id="voice">
    <source src="https://www.soundjay.com/human/sounds/cheering-01.mp3">
</audio>

<script>
const msg = document.getElementById("message");
const balloons = document.getElementById("balloons");
const voice = document.getElementById("voice");

/* ---- New Year Time Check ---- */
function checkNewYear(){
    const now = new Date();
    if (now.getHours() === 0 && now.getMinutes() === 0 || now.getHours() > 0){
        showGreeting();
    }
}
function showGreeting(){
    balloons.style.display = "none";
    msg.style.display = "block";
    voice.play();
    startFireworks();
}
checkNewYear();
setInterval(checkNewYear, 1000);

/* ---- Fireworks ---- */
const canvas = document.getElementById("fireworks");
const ctx = canvas.getContext("2d");
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

let particles = [];

function startFireworks(){
    setInterval(()=>{
        for(let i=0;i<80;i++){
            particles.push({
                x: canvas.width/2,
                y: canvas.height/2,
                vx: (Math.random()-0.5)*8,
                vy: (Math.random()-0.5)*8,
                life: 100
            });
        }
    }, 700);
}

function animate(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    particles.forEach((p,i)=>{
        p.x += p.vx;
        p.y += p.vy;
        p.life--;
        ctx.fillStyle = "hsl(" + Math.random()*360 + ",100%,50%)";
        ctx.fillRect(p.x,p.y,3,3);
        if(p.life<=0) particles.splice(i,1);
    });
    requestAnimationFrame(animate);
}
animate();
</script>

</body>
</html>